To compile and run the file: 

Be in the directory where Makefile, hw2.c, P.fa, and T.fa files are in.
Then in the command line type "make"
Then execute using "./hw2 -i T.fa -p P.fa"

This will print the results in the command line.

ASSUMPTIONS ABOUT THE HW:

* For the Rabin-Karp algo, I displayed the number of hash value comparisons and character comparions seperately,
Then, to find the best algorithm I used their sum for Rabin-Karp.

* I displayed the best algo with respect to character comparisons.

Çerağ Oğuztüzün
21704147